<UserControl x:Class="TextSeeker.TextSeekerMVVM.PreviewControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TextSeeker.TextSeekerMVVM"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar>
            <Button x:Name="PreviousResultButton">
                <Image Source="/Resources/PreviousIcon.png" />
            </Button>
            <Button Name="NextResultButton">
                <Image Source="/Resources/NextIcon.png" />
            </Button>

            <Button x:Name="ToggleModeButton">

            </Button>
            
        </ToolBar>
    </Grid>
</UserControl>
